import 'package:flutter/material.dart';

/// Const app widget keys
/// Currently only needed for tests
class ConstWidgetKeys {
  /// Widget key for password form field
  static const passwordFormField = Key("PasswordTextFormFieldKey");

  /// Widget key for phone number form field
  static const phoneFormField = Key("PhoneFormFieldKey");
}
